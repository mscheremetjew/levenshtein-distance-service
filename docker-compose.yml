version: "3.1"
services:
  distance_calculator:
    stdin_open: true
    build:
      context: .
      dockerfile: Dockerfile
    command: adev runserver -p 8002
    ports:
      - "8001:8001"
    environment:
      # Logging
      - CONSOLE_LOGGING_HANDLER_MIN_LEVEL=INFO
      - CUSTOM_LOGGING